"""Adapters: the concrete implementations of the ports.

These are the only place that touches the outside world (data stores, I/O,
payment mechanics). They depend on the domain; the domain never depends on them.
"""
