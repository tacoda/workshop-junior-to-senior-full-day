[pytest]
# The shipped suite lives in tests/. reference/ is the answer key for the refactor
# lab and has its own suite — run it from inside reference/, not from here.
testpaths = tests
norecursedirs = reference
